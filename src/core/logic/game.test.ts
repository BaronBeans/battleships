import { Game } from "./game";

// jest.mock("./game");

// describe("Game tests", () => {
//   it("throws if no player name is specified", () => {
//     const nothing = undefined;
//     // const game = new Game(nothing);
//     expect(new Game(nothing)).toThrowError("No player specified");
//   });
// });
